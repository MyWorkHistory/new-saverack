/** Rows-per-page choices for CRM data tables (must stay in sync with API caps). */
export const PER_PAGE_OPTIONS = [25, 50, 100, 200, 500];

export const DEFAULT_PER_PAGE = 25;

/** Shopify list pages: orders, inventory, locations, packaging. */
export const LIST_PAGE_SIZE_OPTIONS = [25, 50, 100, 250, 500, 1000];

export const LIST_PAGE_SIZE_DEFAULT = 25;
